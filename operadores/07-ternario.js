// Expresion ? 'Si es true' : 'si es false'

/*
Operador ternario: es un operador condicional que permite evaluar, una expresión basada en una condición
 y devolver un valor u otro dependiendo de si la condición es verdadera o falsa, es lo mismo que 
 expresar estructuras if-else simples en una sola línea. En esencia, toma tres operandos: una condición, 
 un valor si la condición es verdadera, y un valor si la condición es falsa. 

*/

let edad = 17;

let acceso = edad >= 17 ? 'Permitir ingreso ' : 'No puede ingresar';

console.log(acceso)
